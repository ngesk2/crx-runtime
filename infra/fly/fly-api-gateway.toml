# fly.toml for API Gateway

app = "ping-api-gateway"
primary_region = "iad"

[build]
  dockerfile = "../deploy/Dockerfile"

[env]
  API_HOST = "0.0.0.0"
  API_PORT = "8000"
  GATEWAY_MODE = "true"
  OTEL_SERVICE_NAME = "ping-api-gateway"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [[http_service.checks]]
    interval = "15s"
    timeout = "10s"
    grace_period = "5s"
    method = "GET"
    path = "/health"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024

[deploy]
  strategy = "rolling"

[metrics]
  port = 9464
  path = "/metrics"
