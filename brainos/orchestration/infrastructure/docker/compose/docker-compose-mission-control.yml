version: '3.8'

services:
  # Mission Control API
  mission-control:
    build:
      context: ../../../../../
      dockerfile: brainos/orchestration/infrastructure/docker/Dockerfile.mission-control
    container_name: ping-mission-control
    restart: unless-stopped
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: crx_runtime
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      QDRANT_URL: ${QDRANT_URL}
      QDRANT_API_KEY: ${QDRANT_API_KEY}
      QDRANT_COLLECTION: constitutional_memory
      OLLAMA_BASE_URL: http://ollama:11434
      YAHOO_EMAIL: ${YAHOO_EMAIL}
      YAHOO_APP_PASSWORD: ${YAHOO_APP_PASSWORD}
    ports:
      - "8000:8000"
    volumes:
      - ../../../../../vault:/app/vault
    networks:
      - brain_internal
    depends_on:
      - postgres
      - qdrant
      - ollama

  # PostgreSQL - Canonical State
  postgres:
    image: postgres:15-alpine
    container_name: brain-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../../../../../brainos/orchestration/constitutional/canonical_state/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
    networks:
      - brain_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Qdrant - Vector Projection
  qdrant:
    image: qdrant/qdrant:latest
    container_name: brain-qdrant
    restart: unless-stopped
    environment:
      QDRANT__SERVICE__API_KEY: ${QDRANT_API_KEY}
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - brain_internal
    ports:
      - "6333:6333"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Ollama - AI Model Server
  ollama:
    image: ollama/ollama:latest
    container_name: brain-ollama
    restart: unless-stopped
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - brain_internal
    ports:
      - "11434:11434"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Open WebUI - Presentation Layer
  openwebui:
    image: ghcr.io/open-webui/open-webui:latest
    container_name: brain-openwebui
    restart: unless-stopped
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
      WEBUI_SECRET_KEY: ${WEBUI_SECRET_KEY:-default-secret-key-change-in-production}
    depends_on:
      - ollama
      - mission-control
    networks:
      - brain_internal
    ports:
      - "3000:8080"
    volumes:
      - openwebui_data:/app/backend/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  brain_internal:
    driver: bridge

volumes:
  postgres_data:
  qdrant_data:
  ollama_data:
  openwebui_data:
