# Constitutional Runtime - Python Dependencies

# Core dependencies
pydantic>=2.0.0
sqlalchemy>=2.0.0
asyncpg>=0.28.0
alembic>=1.11.0

# Configuration (Phase 14: REPLACE)
pydantic-settings>=2.0.0

# Async filesystem operations (Phase 12: REPLACE IMMEDIATELY)
aiofiles>=23.2.0

# S3 storage (Phase 12: REPLACE IMMEDIATELY)
boto3>=1.28.0
botocore>=1.31.0

# Async HTTP client
httpx>=0.24.0

# NATS transport (Phase 13: REPLACE IMMEDIATELY)
nats-py>=0.0.0

# API framework
fastapi>=0.100.0
uvicorn>=0.23.0

# Structured logging (Phase 14: REPLACE)
structlog>=23.0.0

# Metrics (already using prometheus-client)
prometheus-client>=0.19.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
