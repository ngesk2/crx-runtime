# Intent Registry

This directory contains the constitutional intent surface for the repository.
Each intent file states why an authority, capability, or implementation exists.

## Structure

- Each file is a YAML intent manifest.
- The registry is machine-readable and used by the semantic graph builder.
- Intent files are the canonical entry point for navigation before filesystem lookup.
